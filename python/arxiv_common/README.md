# arxiv_common

Shared library for the Python side: pydantic mirrors of `schemas/messages.v1.json`, schema validation, the OpenAI-compatible inference client, and Postgres + pgvector upserts and search.
